version: '2'
services:
  pp-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/PP/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=PP
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
